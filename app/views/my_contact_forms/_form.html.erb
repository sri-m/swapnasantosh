<%= form_for(@my_contact_form) do |f| %>
  <% if @my_contact_form.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@my_contact_form.errors.count, "error") %> prohibited this my_contact_form from being saved:</h2>

      <ul>
      <% @my_contact_form.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :address %><br>
    <%= f.text_area :address %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
